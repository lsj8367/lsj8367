#include <stdio.h> //헤더파일 (전처리)

int main() //C언어에서 시작과 끝을 담당하는 기본 함수
{ //블록시작
    // int a, a1, 1a;
    // int _a, __a, ___;
    // int if;
    int __,a = 3; //변수명 지정
    char b = 'A'; //아스키코드
    __ = a + 6; // __ = 9;
    printf("%d\n", __); //%d(숫자) %c (문자)
    printf("%c\n", b);
    printf("%d\n", b);
    printf("%d\n", a);
    
    return 0;
}